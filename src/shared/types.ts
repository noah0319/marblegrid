// Shared types between main, renderer, and overlay.
// Real event/participant types land in Phase 1 alongside the CSV parsers —
// see 01 Architecture & Design/(C) implementation-plan.md for the confirmed schemas
// this will be built against (race/tilt events+participants, royale, seasons, etc.).
export {}
